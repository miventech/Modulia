{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "title": "OCR local",
  "type": "object",
  "properties": {
    "enabled": {"type": "boolean", "default": true, "description": "Activa el OCR después de reiniciar."},
    "tesseract_command": {"type": "string", "default": "tesseract", "description": "Ruta o comando de Tesseract OCR."},
    "language": {"type": "string", "default": "spa+eng", "description": "Idiomas instalados en Tesseract, por ejemplo spa+eng."},
    "max_file_mb": {"type": "integer", "default": 20, "description": "Tamaño máximo de imagen o PDF."},
    "pdf_dpi": {"type": "integer", "default": 200, "description": "Resolución usada para convertir páginas PDF a imagen."},
    "retain_input": {"type": "boolean", "default": true, "description": "Conserva el archivo original en data/inbox."}
  },
  "additionalProperties": false
}

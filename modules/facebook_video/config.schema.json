{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "title": "Configuración de video de Facebook",
  "type": "object",
  "properties": {
    "enabled": {
      "type": "boolean",
      "default": true,
      "description": "Activa el módulo al iniciar ModulAI."
    },
    "output_dir": {
      "type": "string",
      "default": "downloads/facebook",
      "description": "Directorio relativo a data_dir donde se guardan los videos."
    },
    "ffmpeg_location": {
      "type": "string",
      "default": "",
      "description": "Ruta opcional a ffmpeg.exe o a su carpeta."
    }
  },
  "additionalProperties": false
}
